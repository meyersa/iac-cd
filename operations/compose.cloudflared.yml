services:
  cloudflared:
    image: cloudflare/cloudflared
    container_name: cloudflared
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    command:
      - tunnel
      - run
    env_file:
      - ./compose.cloudflare.env
    networks:
      - proxy

networks:
  proxy: