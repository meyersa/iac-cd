apiVersion: 1
datasources:
  - name: Mimir
    uid: mimir
    type: prometheus
    access: proxy
    url: http://mimir:9009/prometheus
    isDefault: true
    jsonData:
      prometheusType: Mimir
    editable: ${GRAFANA_EDITABLE}
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: ${GRAFANA_EDITABLE}