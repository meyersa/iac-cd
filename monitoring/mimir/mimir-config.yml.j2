target: all
multitenancy_enabled: false

server:
  http_listen_port: 9009

common:
  storage:
    backend: filesystem
    filesystem:
      dir: /mimir/data

blocks_storage:
  storage_prefix: blocks

ruler_storage: 
  storage_prefix: ruler 

ingester:
  ring:
    replication_factor: 1
    kvstore:
      store: inmemory

limits:
  max_label_name_length: 1024
  max_label_value_length: 2048
  max_label_names_per_series: 30
  compactor_blocks_retention_period: {{ '168h' if ansible_hostname == 'dev' else '0s' }}

