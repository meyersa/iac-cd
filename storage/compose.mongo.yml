services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    env_file:
      - ./compose.mongo.env
    volumes:
      - ${DATA_DIR}/mongo:/data/db
    networks:
      - mongodb

networks:
  mongodb: