---
name: Auto Merge

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: meyersa/iac-ci/.github/workflows/reusable-automerge.yml@main
    secrets: inherit